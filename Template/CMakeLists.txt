# Project generated by gocpp.dev.

cmake_minimum_required(VERSION 3.20)

project(
  ${GOCPP_DEV_APP_NAME}
  LANGUAGES   C CXX
  VERSION     0.1.0
  DESCRIPTION "My C++ Application"
)

set(AppName ${CMAKE_PROJECT_NAME})

add_executable(${AppName})

# Specify the app's source files.
target_sources(${AppName}
  PRIVATE
    src/main.cpp
)

# Require at least C++17.
target_compile_features(${AppName} PRIVATE cxx_std_17)

include(cmake/CompilerSetup.cmake)
include(cmake/CPM.cmake)

# Set up all compiler flags for the app.
setup_compiler_for(${AppName})

# Load the dependencies.
# For a reference, please visit: https://github.com/cpm-cmake/CPM.cmake
CPMAddPackage("gh:fmtlib/fmt#11.0.2")
CPMAddPackage("gh:microsoft/GSL@4.0.0")

# Link with the dependencies.
target_link_libraries(${AppName} PRIVATE
  fmt::fmt-header-only
  GSL
)
